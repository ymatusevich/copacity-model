%td= link_to user.email, [:admin, user]
%td= user.name
%td= user.role
- if policy(:user).destroy?
  %td= link_to 'Edit', edit_admin_user_path(user)
- if policy(:user).destroy?
  %td= link_to 'Delete', admin_user_path(user), data: { confirm: 'Are you sure?' }, method: :delete, class: 'button-xs'
